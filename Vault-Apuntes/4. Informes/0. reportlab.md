Para generar **informes en formato PDF**, se usara la libreria reportlab. Es necesario istalarla

![[Pasted image 20251113222757.png]]

Ahora crearemos un archivo `reports.py` donde haremos una prueba de reporte

```python
class Printer():
    def reportListaClientes():
        try:
            # Creamos el canvas en la carpeta reports
            var.report = canvas.Canvas("informes/report.pdf")
            # Dibujamos el string Listado de clientes en esa posicion x y
            var.report.drawString(100, 750, 'Listado de clientes')
            # Guardamos
            var.report.save()

            # Recorremos la carpeta informes abriendo todos los ficheros PDF
            rootPath = ".\\informes"
            for file in os.listdir(rootPath):
                if file.endswith(".pdf"):
                    os.startfile(os.path.join(rootPath, file))

        except Exception as e:
            print("Error al generar report: ", e)
```

**Para que funcione**, debemos crear la carpeta ``informes`` manualmente, o hacer que el programa lo haga por si solo, pero no tengo tantas ganas de comerme ahora la cabeza.

También crearemos una **variable global** (en [[var.py]]) para que se pueda acceder a un report en proceso de creacion.

```python
report = None
```